#!/bin/bash

# Update packages by default.
yum -y update

# This default script does not contain any functionallity. You can place a `user_data.sh` in your root module to add features. See the directories in `examples` for an example.
